[package]
name = "jet-core"
version = "0.1.0"
description = "Shared pricing, analytics, and parsing library for JET"
edition = "2021"
rust-version = "1.77.2"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
statrs = "0.18"
chrono = "0.4"
thiserror = "2"
